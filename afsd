i need a new feature added to the NyfsModdingTools.

3 new things for when adding dependencies.

requiredMod() just normal implementation
embeddedMod() this will jarJar or include (neoforge(forge) or fabric)
optionalMod() just normal implementation

the reason for the 2 different required/optional doing the same thing is because there is an additional feature.

It will at build time modify (neo.)mod.toml or fabric.mod.json, so ONLY the file put into the jar, not the one in the project.
it will be adding the dependency stuff to it.

so iun fabric it just adds a line to the block depend or recommendds
"modid": ">=version"

for (neo.)mods.toml it will add
[[dependencies.modid]]
modId="modid"
mandatory=true/false
versionRange="[version,)"